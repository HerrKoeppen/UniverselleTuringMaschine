
    String aktuellerZustand;
    
    private void setAnfangszustand()
    {
    	aktuellerZustand = arrayListAnfangszustand.get(0);
    }
    	
    	
    
    HashMap hashUebergang = new HashMap();
    private void erzeugeHashmap()
    {
        Iterator<String> it = arrayListUebergangsfunktion.iterator();
        while(it.hasNext())
        {
            String uebergangsfunktion = it.next();
            String[] parts = uebergangsfunktion.split("|");
            String part1 = parts[0];
            String part2 = parts[1];
            hashUebergang.put(part1|part2);
            
        }
    private void uebergang(hashUebergang)
    {
    	while(laufen)
	{
                String schluessel = aktuellerZustand + "," + bandLesen();
		String uebergangAlleInformationen = hashUebergang.get(schluessel);
		String[] uebergang = uebergangAlleInformationen.split(,);
		String schrBandsymbol = uebergang[0];
                zeichenHinzufuegen(schrBandsymbol);
		String kopfrichtung = uebergang[1];
                schreibkopfBewegen(kopfrichtung);
		String neuerZustand = uebergang[2];
                uebergangZustand(neuerZustand);
	}	    
    }
    private  void uebergangzustand(neuerZustand);
             String  aktuellerZustand = neuerzustand;
